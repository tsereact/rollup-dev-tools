{
  "private": true,
  "type": "module",
  "packageManager": "yarn@3.2.4",
  "workspaces": [
    "experiments/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "yarn workspaces foreach -ipv --include '@tsereact/rollup-plugin-*' --include '@tsereact/tool-*' exec rollup -c rollup.config.js"
  },
  "devDependencies": {
    "typescript": "^4.8.4"
  }
}
